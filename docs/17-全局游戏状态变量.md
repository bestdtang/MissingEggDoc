# 17 全局游戏状态变量

> **用途**：对话树状脚本、质询锁、环境交互的**唯一变量名对照表**。改任一 NPC 对话脚本前须先查本表；新增变量须先在本表登记，再写入脚本【变量】块。  
> **类型**：仅 `bool` 与 `int`。  
> **关联**：[13-玩家线索与交互点总表](./13-玩家线索与交互点总表.md)（E 点写入）、[09-侦探笔记本](./09-侦探笔记本.md)（条目文案，不设对话 bool）。

---

## 17.1 命名规则

| 规则 | 示例 |
| --- | --- |
| 有 E 代号的环境状态 | `{E##}_{动作}` · `E06_ViewNeedLadder`、`E06_LadderPlaced`；**仅**环境交互（或本表明写的对话例外）写入 |
| 同 E 点多状态 | 后缀区分 · 缺梯 / 借梯 / 架梯 各一条 |
| NPC 专属阶段 | `{Role}Status` · `DogStatus` |
| 大黄对话写入 | `Dog_*` · `Dog_BlackCatSummoned`（**非** E 点 bool，勿与 `E28_ViewTreeScratch` 混读） |
| 第二章 · 大黄一次性台词 | `RedRoof_*` · `RedRoof_IntroShown` 等（防重复必播；**不含**章节解锁——解锁读 `DogStatus==4`） |
| 跨 NPC 黑猫线 | `BlackCat_*` |
| 聚合计数 | `{Name}Count` · `TreeClueCount`、`ChickTraceCount` |

**不写进本表的**

- 笔记本条目 bool（由 `09` 文案 + 上述 E 变量推导展示；对话树只读 E 变量，不另建 `Note_*` bool）。
- 奶酪碎 `CheeseCount`（经济计数，见 `13` §13.5；老鼠脚本引用，不进质询锁树）。

**对话脚本写法**

- 分支：`` 「台词」`（条件）→ 节点 ``
- 节点末：`` 【变量】` · `Var = value`（仅列本节点**新写入**项；**全文播毕**后统一写入，再执行 `→` 出口）``

**跨文档锁口径（大黄树状样章为准）**

| 策划说法 | 变量条件 |
| --- | --- |
| 大黄 2-G 完成 / 阶段④ / 【嗅觉追踪证词】/ 已进入第二章 | `DogStatus == 4`（仅 `2-G` 写入） |

---

## 17.2 环境线索变量（E 点）

写入来源均为 **环境交互**（`13`）；对话脚本只**读取**，除非本表「写入方」列明对话节点。

### 第一章 · 鸡舍 / 池塘 / 谷仓

| 变量 | 类型 | 含义 | 写入来源 | 主要引用 |
| --- | --- | --- | --- | --- |
| `E01_ViewCharcoal` | bool | 已调查短木炭 | **E01** | 小鸡 `ChickTraceCount` |
| `E02_ViewFeather` | bool | 已调查散落羽毛 | **E02** | 小鸡 `ChickTraceCount` |
| `E03_Overheard` | bool | 已触发偷听 | **E03** | 小鸡 `ChickTraceCount` |
| `E05_GrainSoakGet` | bool | 已取【谷物泡水】 | **E05**（需 `DogStatus >= 2`） | 大黄 2-C / 2-C′ |
| `E06_ViewNeedLadder` | bool | 已发现入口缺梯 | **E06** 首次 | 大黄 2-A 短段、2-C′、2-E 菜单 |
| `E06_LadderBorrowed` | bool | 已借得【木梯】 | 大黄 `2-E`【变量】 | 大黄借梯菜单；**E06** 架设前置 |
| `E06_LadderPlaced` | bool | 已在入口架梯 | **E06** 持梯再次交互 | 谷仓平台跳跃解锁 |
| `E07_ViewNapSpot` | bool | 已发现【谷仓午睡点】 | **E07** | 大黄 `2-F`；黑猫薄荷鱼线 |
| `E08_ViewBurnMark` | bool | 已发现【烧焦稻草/皮毛】 | **E08** | 黑猫薄荷鱼线；乌鸦追问 |
| `E10_NoteIllust` | bool | 笔记本第二页已有【屋顶白石头】插图（`E10` 交互后入册） | **E10** | 大黄 `2-G` / 小鸡 `[展示笔记本]` |
| `E12_ViewGreenPad` | bool | 已发现【蛙身下绿垫】 | **E12** | 悲伤蛙薄荷鱼线 |
| `E17_ViewEmptyBucket` | bool | 已调查门旁空水桶 | **E17** | 黑猫案情汇报线 |
| `E18_ViewBootprints` | bool | 已调查雨靴泥脚印 | **E18** | 黑猫案情汇报线 |
| `E28_ViewTreeScratch` | bool | 已调查橡树根抓痕 | **E28** | `TreeClueCount` |
| `E30_BlackFurSeen` | bool | 已在 E30 见过黑色细毛 | **E30** | **E16** 玩家台词分支（非计数） |

### 第二章 · 红顶屋外

| 变量 | 类型 | 含义 | 写入来源 | 主要引用 |
| --- | --- | --- | --- | --- |
| `E13_ViewDoorBlocked` | bool | 已确认正门进不去 | **E13** | 大黄 3-A′、3-C；黑猫 |
| `E14_ViewCatDoor` | bool | 已调查猫门 | **E14** | `TreeClueCount` |
| `E15_ViewFoodBowl` | bool | 已调查门外食碗 | **E15** | `TreeClueCount` |
| `E16_ViewFur` | bool | 已调查门边兽毛 | **E16** | `TreeClueCount`；台词随 `E30_BlackFurSeen` |

> **E04 初见**：不设 `E04_*` bool。谷仓旁初见 = 大黄 `2-A` 将 `DogStatus` 从 `1` 置 `2`；笔记三条由 `2-A`【变量】注明刷新，文案见 `09`。

---

## 17.3 聚合计数

| 变量 | 类型 | 含义 | 更新规则 | 主要引用 |
| --- | --- | --- | --- | --- |
| `ChickTraceCount` | int | 小鸡搞事碎片条数 | `E01`/`E02`/`E03` 各首次 +1 | 小鸡水怪说（≥2） |
| `TreeClueCount` | int | 下树线索条数 | `E14`/`E15`/`E16`/`E28` 各首次 +1（**不含 E13**） | 大黄 3-C（≥2） |

实现侧可用四个 bool 派生计数；对话条件统一写 `*Count >= N`。

---

## 17.4 NPC 状态变量

| 变量 | 类型 | 取值 | 写入方 | 主要引用 |
| --- | --- | --- | --- | --- |
| `DogStatus` | int | `1` 完全睡 · `2` 半睡 · `3` 清醒 · `4` 嗅觉追踪（`2-G` 后） | 大黄 `2-A`→2 · `2-C`→3 · `2-G`→4 | 大黄全线；**E05** 解锁（`>=2`） |
| `Dog_BlackCatSummoned` | bool | 大黄 `3-C` 已摇橡树召唤黑猫 | 大黄 `3-C` | 大黄红顶 hub / `3-D` / `3-E` |
| `BlackCat_CaseLineDone` | bool | 案情汇报线完成 | 黑猫脚本（需 `E17`+`E18`） | 大黄 3-E |
| `BlackCat_MintFishLineDone` | bool | 薄荷鱼线完成 | 黑猫脚本（需 `E07`+`E08` 等） | 大黄 3-E |
| `BlackCat_Entered` | bool | 已进猫门 | 大黄 `3-E` | 大黄 3-F |

---

## 17.5 章节与演出 flag

| 变量 | 类型 | 含义 | 写入来源 | 主要引用 |
| --- | --- | --- | --- | --- |
| `RedRoof_IntroShown` | bool | 大黄 `3-A` 抵达嗅确认必播已出 | 大黄 `3-A` | 大黄 `3-A` 一次性 |
| `RedRoof_DoorHintShown` | bool | 大黄 `3-A′` 正门碰壁台词已出 | 大黄 `3-A′` | 大黄 `3-A′` 一次性 |
| `RedRoof_RoofWaitShown` | bool | 大黄 `3-F` 攻顶前加油台词已出 | 大黄 `3-F` | 大黄 `3-F` 一次性 |

> **第二章解锁**：不设 `RedRoof_Arrived`。`2-G` 仅写 `DogStatus=4`；红顶屋外全线及跨 NPC「已进入第二章」锁统一读 **`DogStatus == 4`**。

---

## 17.6 周目

| 变量 | 类型 | 含义 | 写入来源 | 主要引用 |
| --- | --- | --- | --- | --- |
| `NGPlus` | bool | 二周目 | 漫画合本后 | 全 NPC 回访段 |

---

## 17.7 登记流程（改脚本时）

1. 查本表：条件里出现的变量须已存在；写入新变量须先增行。  
2. 写树状脚本：入口只读；节点末【变量】只列变更。  
3. 环境点改锁 → 同步 `13` + 本表 + 涉及 NPC 脚本。  
4. 笔记本新条目 → 改 `09`；若需质询锁，增加对应 `E##_` bool，**不**建 `Note_*`。

---

## 17.8 大黄树状脚本速查（与样章对齐）

| 节点 | 进入条件（读取） | 【变量】写入 |
| --- | --- | --- |
| 前置 | `DogStatus==1` | — |
| 2-A | `DogStatus==1` | `DogStatus=2` + 笔记三条（见 `09`） |
| **半睡回访入口** | `DogStatus==2`；`E05_GrainSoakGet` 路由 | — |
| 2-C | `DogStatus==2` && `E05_GrainSoakGet` | `DogStatus=3` |
| 2-C′ | `DogStatus==2` && `!E05_GrainSoakGet` | — |
| 2-D hub | `DogStatus==3` | — |
| 2-E | `E06_ViewNeedLadder` && `!E06_LadderBorrowed` | `E06_LadderBorrowed=true` |
| 2-F | `DogStatus==3` && `E07_ViewNapSpot` | — |
| 2-G | `DogStatus==3` && `E10_NoteIllust` | `DogStatus=4` |
| 3-A | `DogStatus==4` && `!RedRoof_IntroShown` | `RedRoof_IntroShown=true` |
| 3-A′ | `DogStatus==4` && `E13_ViewDoorBlocked` && `!RedRoof_DoorHintShown` | `RedRoof_DoorHintShown=true` |
| 红顶 hub | `DogStatus==4` && `!Dog_BlackCatSummoned` | — |
| 3-C | `E13_ViewDoorBlocked` && `TreeClueCount>=2` && `!Dog_BlackCatSummoned` | `Dog_BlackCatSummoned=true` |
| 3-D | `Dog_BlackCatSummoned` && `!BlackCat_Entered` | — |
| 3-E | `Dog_BlackCatSummoned`；黑猫双线 + `!BlackCat_Entered` | `BlackCat_Entered=true` |
| 3-F | `Dog_BlackCatSummoned` && `BlackCat_Entered` && `!RedRoof_RoofWaitShown` | `RedRoof_RoofWaitShown=true` |
| NGPlus 回访 | `NGPlus` | — |

**写入节点共 9 处**：`2-A` · `2-C` · `2-E` · `2-G` · `3-A` · `3-A′` · `3-C` · `3-E` · `3-F`。

---

*关联文档：[13-玩家线索与交互点总表](./13-玩家线索与交互点总表.md)、[16-NPC对话脚本书写守则](./16-NPC对话脚本书写守则.md)、[大黄-对话脚本-树状样章](./characters/大黄-对话脚本-树状样章.md)*
